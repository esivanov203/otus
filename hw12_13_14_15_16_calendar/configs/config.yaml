logger:
 level: "INFO"
 type: "json" # or text

server:
  host: "0.0.0.0"
  port: 8080

storage:
  type: "memory"
  dsn: ""
#  type: "sql"
#  dsn: "postgres://user:pass@localhost:5432/calendar?sslmode=disable"

